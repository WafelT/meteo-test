# Магазин открыток - Тестовое задание

SPA-магазин открыток с мок-данными и WebSocket для онлайн-обновлений.

📹 preview.mov - демо использование

## 🚀 Запуск

```bash
npm install
npm start
```

Приложение будет доступно по адресу: `http://localhost:5173`

> Команда `npm start` запускает WebSocket сервер и frontend приложение одновременно.

## 📁 Архитектура

**Моки:**
- `src/mocks/` - MSW handlers для REST API (работает только в development)
- `websocket-server.js` - Node.js сервер для WebSocket событий (порт 3001) ка

**WebSocket:**
- `src/services/websocket.ts` - клиент Socket.IO
- События: `product.updated` (обновление цены/наличия товаров каждые 8 секунды)

**Состояние (Pinia stores):**
- `src/stores/auth.ts` - аутентификация
- `src/stores/products.ts` - каталог, фильтры, пагинация
- `src/stores/cart.ts` - корзина с оптимистичными обновлениями

**Структура компонентов:**
- `src/views/` - страницы (Login, Catalog, Product, Cart, Checkout)
- `src/components/widgets/` - сложные компоненты (Header, ProductFilters, CheckoutForm)
- `src/components/features/` - бизнес-логика (ProductCard, CartItem, ProductDetails)
- `src/components/ui/` - переиспользуемые UI компоненты (UiToast, UiModal, UiPagination)
- `src/components/layout/` - компоненты макета (AppNavigation)
- `src/composables/` - переиспользуемая логика (useToast, useTestingFeatures)

## ✅ Реализованные функции

### Страницы
- ✅ Вход (`/login`) - мок-аутентификация с сохранением сессии
- ✅ Каталог (`/catalog`) - список товаров с фильтрами, сортировкой, пагинацией
- ✅ Товар (`/product/:id`) - детальная информация о товаре
- ✅ Корзина (`/cart`) - управление товарами в корзине
- ✅ Оформление (`/checkout`) - форма заказа с подтверждением

### REST API эндпоинты (моки)
- ✅ `POST /api/auth/login` - вход
- ✅ `POST /api/auth/logout` - выход
- ✅ `GET /api/products` - список товаров с фильтрами
- ✅ `GET /api/products/:id` - детали товара
- ✅ `GET /api/cart` - получить корзину
- ✅ `POST /api/cart/add` - добавить в корзину
- ✅ `POST /api/cart/update` - обновить количество
- ✅ `POST /api/cart/remove` - удалить из корзины
- ✅ `POST /api/checkout` - оформить заказ

## ⏱️ Время разработки

~5-6 часов

## 🎯 Сложности

1. **WebSocket интеграция** - настройка Socket.IO сервера и клиента, обработка событий `product.updated`, синхронизация состояния корзины при изменениях товаров


## 🔮 Что можно улучшить

1. **Тестирование** - добавить unit (Vitest) и e2e (Playwright) тесты
2. **Валидация форм** - более детальная валидация с библиотекой (VeeValidate)
3. **Анимации** - плавные переходы между страницами и состояниями (Vue Transition)
4. **Кэширование** - кэширование товаров для улучшения производительности
6. **i18n** - интернационализация (vue-i18n)
7. **Темная тема** - поддержка темной темы с переключением
8. **Виртуализация списков** - для больших каталогов товаров